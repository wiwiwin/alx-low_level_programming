#include"main.h"
#include<string.h>
/**
 * _strlen - function return the length of a string as an integer value
 * @s: string pointer
 * Return:return the lenght of a string as integer value
 */
int _strlen(char *s)
{
	return (strlen(s));
}
