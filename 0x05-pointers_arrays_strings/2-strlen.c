#include"main.h"
#include<string.h>
/**
 * int _strlen- a function that returns the length of a string
 * @s: string pointer 
 *Retern; length of a string 
 */
int _strlen(char *s)
{
	return (strlen(s));
}
