#include<stdio.h>
/**
 * main - prints all single digit numbers of base 10
 *
 * Retun: Always (Success)
 */
int main(void)
{
	int n;

	n = 0;
	while (n < 10)

		{

		putchar(n);
		n++;

		}
	putchar('\n');

	return (0);
}
